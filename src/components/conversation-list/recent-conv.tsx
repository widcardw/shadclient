import type { Component } from 'solid-js'

/**
 * 最近会话列表，包含了私聊和群聊的一个个小的实例
 */
const RecentConversationList: Component = () => {
  return <div>recent conversation list</div>
}

export default RecentConversationList
